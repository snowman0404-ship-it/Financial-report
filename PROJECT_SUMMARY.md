# Financial Report ウェブアプリ — プロジェクト概要

---

## 背景・作成の目的

共同投資先企業の財務状況を四半期ごとにPowerPointで分析していたが、そのデータ収集・整理に多くの時間を要していた。本ツールはその課題を解消するために作成した。

| 課題 | 解決したこと |
|------|------------|
| 四半期ごとのPPT分析データ取得が手作業で非効率 | SEC EDGARから自動取得・整形 |
| 過去データを遡って確認しにくい | 直近24期分の10-Q/10-K を一覧から選択可能 |
| 詳細分析の前に財務状況の全体像が掴めない | リスク判定（🟢/🟡🔴）を即座に表示 |
| 担当者変更時の引継ぎが煩雑 | Excelダウンロード＋ウェブUIで誰でも操作可能 |

**期待効果**：業務の優先順位判断・効率化、引継ぎのしやすさ向上

---

## 使い方（ユーザー操作の流れ）

```
① ティッカー入力（例: PARR）
       ↓
② 「決算期リストを取得」ボタン → 10-Q / 10-K の一覧が表示
       ↓
③ 分析したい期を選択 → 「財務分析を実行」ボタン
       ↓
④ 画面上に結果が表示 ＋ Excelダウンロード
```

---

## 取得・表示するデータ

| カテゴリ | 内容 |
|---------|------|
| 損益計算書（P&L） | 売上高・営業費用・営業利益・支払利息・税引前利益・純利益（前年同期比） |
| 貸借対照表（B/S） | 現金・流動資産・流動負債・長期負債・自己資本（前四半期比） |
| 財務比率 | 流動比率・自己資本比率・各項目のQoQ変化 |
| リスク判定 | 流動比率・自己資本比率の閾値に基づき緑/黄/赤で自動判定 |
| バリュエーション | PER・PBR（株価はyfinanceから取得） |
| 株価チャート | 5年間の日次終値 ＋ S&P500比較 |
| MD&A テキスト | 10-Q/10-K の「経営者による分析・考察」セクションを自動抽出 |

---

## システム構成（技術スタック）

```
┌─────────────────────────────────────────────────┐
│              ブラウザ（ユーザー）                  │
└────────────────────┬────────────────────────────┘
                     │ HTTP
┌────────────────────▼────────────────────────────┐
│          Streamlit Web UI (app.py)               │
│  ・Pythonだけで画面・ロジックを一体管理             │
│  ・カスタムCSS でカード型UI / 二言語表示            │
└──────┬─────────────────────┬────────────────────┘
       │ 財務データ取得         │ 株価データ取得
┌──────▼────────┐    ┌────────▼──────────┐
│  SEC EDGAR    │    │  Yahoo Finance    │
│  (公開API)    │    │  (yfinance)       │
│               │    │                   │
│ ① ティッカー→CIK  │    │ ・5年株価チャート  │
│ ② 提出書類一覧    │    │ ・PER / PBR       │
│ ③ XBRLファクト   │    │ ・EPS             │
│ ④ 10-Q/K HTML  │    └───────────────────┘
└───────────────┘
```

---

## データ処理フロー

```
ティッカー入力
    │
    ├─ CIK解決  ──→ SEC APIまたは26社ハードコードテーブル
    │
    ├─ 提出書類一覧取得 ──→ 直近24期の10-Q/10-K
    │
    ├─ XBRLファクト取得 ──→ 全財務タグを一括ダウンロード
    │          ↓
    │    P&L抽出（7タグ候補でフォールバック）
    │    B/S抽出（6タグ候補でフォールバック）
    │    四半期番号・期間長から前年同期・前四半期を自動特定
    │
    ├─ MD&A抽出 ──→ HTML→テキスト変換、Item7/Item2セクションを正規表現で切り出し
    │
    ├─ バリュエーション ──→ yfinance → EPS計算 → EDGAR TTMの3段階フォールバック
    │
    └─ Excel生成（3シート）＋ Streamlit画面表示
```

---

## Excelレポート構成（3シート）

| シート名 | 内容 |
|---------|------|
| Dashboard | 会社情報・財務比率・QoQ変化・リスク判定（色分け） |
| Financial Data | P&L・B/S全項目（変化額・変化率をExcel数式で計算、±20%超は赤ハイライト） |
| MD&A_Text | 抽出済みMD&Aテキスト（Claude/ChatGPTへのコピペ用） |

---

## ファイル構成

| ファイル | 役割 |
|---------|------|
| `app.py` | Streamlit ウェブアプリ本体（約2,000行） |
| `financial_analyzer.py` | CLIスタンドアロン版（ターミナルから直接実行可） |
| `requirements.txt` | 依存パッケージ（streamlit / pandas / requests / openpyxl / yfinance / plotly） |

---

## 主な設計上の工夫

- **タグフォールバック**：XBRLのタグ名は企業・年度ごとに異なるため、複数候補を順番に試して取得
- **リトライ処理**：SEC APIの429（レート制限）に対し指数バックオフ（2→4→8→16秒）で自動リトライ
- **デモモード**：ネットワーク不要のデモデータで動作確認可能
- **二言語対応**：UI・Excel・リスク判定メッセージを日本語＋英語で併記

---

*対象企業：米国上場企業全般（デフォルトはPar Pacific Holdings / PARR）*
*データソース：SEC EDGAR（公開情報） ＋ Yahoo Finance*
